<% if user.photo.attached? %>
  <%= cl_image_tag user.photo.key, class: "avatar"%>
<% elsif target %>
  <%= image_tag "center_mark.png", height: 35, width: 30 %>
<% else %>
  <%= image_tag "notimage.png", class: "avatar"%>
<% end %>
